@model HomeViewModel

<partial name="_Carousel"/>

<h2>Os preferido da semana</h2>
<h4>Tá com fome???</h4>

<div class="row">
    @foreach (var lanche in Model.LanchesPreferido)
    {
         <partial name="_LanchesResumo" for="@lanche"/>
    }
</div>